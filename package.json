{
  "name": "midna",
  "private": true,
  "workspaces": [
    "packages/*",
    "web",
    "midna-cli",
    "hardhat-example",
    "web3inbox"
  ],
  "scripts": {
    "dev:web": "yarn workspace web dev",
    "build:web": "yarn workspace @midna/utils build && yarn workspace @midna/db build && yarn workspace @midna/sdk build && yarn workspace @midna/rest build &&  yarn workspace web build",
    "start:web": "yarn workspace web start",
    "db:push": "yarn workspace @midna/db push"
  },
  "packageManager": "yarn@3.6.3",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
